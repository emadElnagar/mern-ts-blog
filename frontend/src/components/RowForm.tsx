import { Fragment } from "react"

const NewContainerForm = () => {
  return (
    <Fragment>
      <form>
        <input type="text" placeholder="category title" />
        <button>+</button>
      </form>
    </Fragment>
  )
}